{
  "name": "contrimon",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "build": "mkdir -p dist && rm -r dist && bun build --outdir=dist --minify index.html",
    "dev": "bun --hot index.html",
    "serve": "bun --hot --port 4000 server"
  },
  "dependencies": {
    "hono": "^4.7.6",
    "preact": "^10.26.5",
    "preact-router": "^4.1.2"
  }
}
