{
  "name": "contrimon",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "build": "bun build --outdir=dist --minify index.html",
    "dev": "bun --hot index.html"
  },
  "dependencies": {
    "preact": "^10.26.5",
    "preact-router": "^4.1.2"
  }
}
